<h3>EmailValidation</h3>

@page "/EmailValidation"
@using System.ComponentModel.DataAnnotations
@rendermode InteractiveServer
@using BusinessLogic.Contracts
@using Models

<h3>Paracambiar la contraseña ingresar</h3>
<EditForm model=@_user OnValidSubmit="HandleEmailValidation" FormName="formEmailValidation">
    <div class="grid-container">
        <div class="form-group">
            <label for="email">Correo: </label>
            <InputText id="email" type="email" class="form-control" @bind-Value="_user.Email" />
        </div>

        <div class="form-group mt-3">
            <button type="submit" class="btn btn-primary">Validar correo </button>
        </div>
    </div>
</EditForm>

@code {
    [Inject]
    private ILoginService _loginService { get; set; }
    [Inject]
    private IEmailService _emailService { get; set; }

    [Inject]
    private NavigationManager _navManager { get; set; }

    [SupplyParameterFromForm(FormName = "formEmailValidation")]
    private User _user { get; set; } = new();

    protected override void OnInitialized()
    {
       
    }

    private void HandleEmailValidation()
    {
        _emailService.SendMail(_user.Email, "ShoppingPOC - Your Password Update Code", "Your code for updating your password is 999666");

               Console.WriteLine("Correo: " + _user.Email);
              _navManager.NavigateTo("/PasswordUpdate");
    }
}
